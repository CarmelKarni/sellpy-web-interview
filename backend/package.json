{
  "type": "module",
  "scripts": {
    "lint": "eslint --ext .js src",
    "start": "PORT=${PORT:-3001} node --watch src/index.js",
    "test": "NODE_OPTIONS=--experimental-vm-modules NODE_ENV=test npx jest --no-coverage"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "express": "^4.19.2"
  },
  "devDependencies": {
    "eslint": "^8.57.0",
    "jest": "^29.7.0",
    "supertest": "^6.3.3"
  }
}
